# Processed Data Directory

This directory contains cleaned and processed data files.

## Files:
- `cleaned_campaign_data.csv` - Cleaned data with calculated metrics
- `dashboard_data.csv` - Data prepared for dashboard visualization
- `campaign_performance.csv` - Aggregated campaign performance data

These files are generated by running the notebooks in the `notebooks/` directory.

